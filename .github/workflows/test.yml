name: test
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      # 测试只依赖 node 内置模块（纯函数已从 sharp 解耦），故不需要 npm install。
      - run: npm test
